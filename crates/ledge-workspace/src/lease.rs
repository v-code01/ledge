//! Durable lease store for ephemeral workspaces (skeleton — Step 2.2).

/// Placeholder lease record (real definition lands in Step 2.2.0).
pub struct Lease;

/// Placeholder lease store (real definition lands in Step 2.2.1).
pub struct LeaseStore;
